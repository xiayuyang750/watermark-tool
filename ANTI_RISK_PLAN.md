# 风控对抗检测与修复计划（计划文档 v0.1）

> 状态：计划稿，待评审后实施
> 日期：2026-08-07
> 适用范围：抖音解析/下载链路的反爬对抗（其他平台接入时按同框架扩展）

## 1. 背景与目标

### 1.1 背景

抖音 Web 端反爬持续升级。本工具在 2026-08 开发中已实际观测到：

- 纯 HTTP + a_bogus 签名方案被**静默拒绝**（200 空响应），`encrypt_data_miss` 码
- 无浏览器环境下页面返回**验证码中间页**
- 注入旧 Cookie 反而被拒（Cookie 绑定浏览器指纹）
- 浏览器方案（Playwright + 页面安全 SDK 自动签名）**当前有效**

风控是动态对抗，现状有效不代表长期有效。需要一套**可重复的检测→定位→修复→验证闭环**，并沉淀对抗策略库，使风控失效时可快速定位与恢复。

### 1.2 目标

1. **检测**：一键诊断当前解析/下载链路是否被风控、被风控在哪个环节
2. **定位**：区分签名层 / 页面层 / 接口层 / 下载层 / 登录态 / IP 频率 等风控维度
3. **修复**：按策略库给出针对性代码修改方案
4. **验证**：修改后重跑诊断 + 真实链接端到端回归，确认通过

## 2. 检测维度与判定标准

| # | 检测项 | 检测方法 | PASS 标准 | FAIL 特征（被风控） |
|---|---|---|---|---|
| D1 | 平台连通性 | GET douyin.com 首页 | 200，页面含正常 JS 壳 | 跳验证码页 / 403 / 429 |
| D2 | 分享链接解析 | 短链重定向链路 | 重定向到 /video/ 或 /note/ 作品页 | 跳到验证码页 / 无法提取 ID |
| D3 | 浏览器解析（主） | Playwright 内 XHR 调 detail | 返回 JSON 且含 aweme_detail | 空响应 / 非 JSON / status_code≠0 |
| D4 | 纯 HTTP 签名（备） | a_bogus/X-Bogus 调 detail | 返回 JSON 且 status_code=0 | 空响应 / 11110 encrypt_data_miss |
| D5 | CDN 下载 | HEAD/GET 媒体直链 | 200 且 content-length 正常 | 403 / 302 验证 / 超时 |
| D6 | 登录态（如需） | 检查 profile Cookie 关键字段 | 含 sessionid/ttwid 等 | 缺失或过期 |
| D7 | IP 频率 | 连续 N 次解析请求 | 无 429 / 无空响应 | 429 / 空响应率上升 |
| D8 | 本地链路 | 后端 health / 前端代理 | 后端 ok、代理 200 | 端口不通 / 代理失败 |

## 3. 诊断工具设计

### 3.1 形态

- **CLI**：`python -m app.anticrawler.diagnose`（主，可脚本化、可入库）
- **API**：`GET /api/v1/diagnose`（供前端诊断页展示）
- 前端诊断页：设置页新增"风控自检"入口（可选，M2 排期）

### 3.2 报告格式

```jsonc
{
  "generated_at": "2026-08-07 18:00:00",
  "summary": { "pass": 5, "warn": 2, "fail": 1 },
  "items": [
    {
      "id": "D3",
      "name": "浏览器解析",
      "level": "FAIL",            // PASS / WARN / FAIL
      "evidence": { "status": 200, "content_type": "text/plain", "body_len": 0 },
      "suggestion": "浏览器方案被静默拒绝：检查页面 SDK 是否注入成功、浏览器指纹、升级 playwright"
    }
  ]
}
```

### 3.3 分级定义

- **PASS**：完全正常
- **WARN**：可用但不稳定（如偶发空响应、响应时间超阈值）
- **FAIL**：被风控，功能不可用

## 4. 风控特征库（判定规则）

| 现象 | 特征 | 判定维度 |
|---|---|---|
| 200 空响应（text/plain, len=0） | 签名无效 / 指纹不匹配 | 签名层 D4 |
| `status_code: 11110 encrypt_data_miss` | 缺签名参数 | 签名层 D4 |
| HTML 标题含"验证码" | 验证码中间页 | 页面/IP 层 D1/D2 |
| HTTP 429 / 403 | 频率限制 / 拒绝 | IP 频率层 D7 |
| 302 → 登录/验证页 | 需登录态 | 登录态 D6 |
| 200 但无 aweme_detail | 静默拦截 | 接口层 D3 |
| CDN 403 / 签名过期 | 直链失效 | 下载层 D5 |

## 5. 对抗策略库（修复方案，按层）

> 按优先级 L0→L5 执行；每层策略独立可回滚。

### L0 基线（默认开启）
- 浏览器方案保持：**常驻浏览器实例**（减少每次冷启动、降低指纹暴露频率）
- 请求限速：默认 1 次/秒级，失败退避（1s/3s/10s/30s）
- UA / Referer / Origin 请求头完整性与一致性

### L1 请求层
- 重试与退避策略抽象（`retry_strategy.py`）
- 完整浏览器请求头（sec-ch-ua、sec-fetch-*、Accept-Language）
- Cookie 会话保持（httpx Client 复用）

### L2 浏览器层
- **持久化 profile**（登录态、指纹长期稳定，替代每次注入）
- 指纹一致性：UA/屏幕/时区/语言与 profile 匹配
- 行为模拟：进入作品页停留、滚动，再取数
- 多实例轮换（并发解析时隔离风控面）

### L3 签名层
- 同步社区最新 a_bogus/X-Bogus 实现（douyin_parse 等，注意 GPL 授权）
- UA 特征码与签名内嵌码对齐
- 签名有效性纳入诊断 D4 常态化监控

### L4 通道层（谨慎，需合规评估）
- 备用解析通道（iesdouyin 接口、移动端接口）
- 本地代理池 / IP 轮换（仅评估后启用）

### L5 登录态
- 需要时扫码登录一次，持久化到 profile
- 登录态失效自动提示重新登录

## 6. 修改-验证闭环流程

```
① 跑诊断 → 记录 FAIL/WARN 项
② 按特征库映射到策略层（L0-L5）
③ 应用对应代码修改
④ 重跑诊断 → 目标项转 PASS 或 WARN
⑤ 端到端回归：真实链接连续下载 3 次全部成功
⑥ 提交代码并记录"现象→根因→策略"到本文档案例库
```

**验收标准**：诊断无 FAIL；真实链接连续 3 次解析+下载成功；WARN 项有明确解释。

## 7. 长期监控

- 每次启动（start 脚本）执行快速自检（D1/D3/D8），WARN/FAIL 自动提示
- 全量诊断（D1-D8）提供手动触发入口
- 风控特征库与案例库随每次对抗持续沉淀

## 8. 风险与合规边界（写死）

- **对抗上限**：不绕过登录/付费墙、不撞库、不暴力枚举、不做内容审核规避
- **频率控制**：默认限速，避免对平台造成压力（个人工具定位）
- **合规**：仅面向合法素材；代理/IP 轮换策略必须合规评估后再启用
- **授权**：签名算法移植注意 GPL/Apache 授权标注（见 douyin_sig.py 头注释）

## 9. 里程碑

| 里程碑 | 交付物 | 验收 |
|---|---|---|
| M0 | 诊断工具（CLI + API + 报告格式） | 诊断 D1-D8 全部可执行、输出分级报告 |
| M1 | 策略库 L0-L2 落地（限速/退避/常驻浏览器/持久化 profile） | 连续 3 次端到端通过 |
| M2 | 验证闭环自动化（一键诊断→修复→回归） | 脚本一键完成 ①-⑥ |
| M3 | 长期自检（启动自检 + 特征库沉淀） | 启动提示 + 案例库 |

## 附：本次实测证据（2026-08-07）

- 纯 a_bogus + 完整参数：200 text/plain len=0（被拒）
- iteminfo 接口：`encrypt_data_miss`
- 页面：`验证码中间页`（iesdouyin share）
- 注入旧 Cookie（无 sessionid，绑定他指纹）：detail 空响应
- 浏览器方案（全新上下文，不注入 Cookie）：detail 正常返回完整 aweme_detail
