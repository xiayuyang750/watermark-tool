# 更新日志（CHANGELOG）

## v0.3.6（2026-08-13）

> 工程化改进：新增三后端可选启动脚本，修复启动脚本在 Windows PowerShell 5.1 下的中文乱码。

### 🛠️ 启动脚本

- 新增 `scripts/start-python.bat` / `start-rust.bat` / `start-go.bat`（双击入口）+ 对应 `.ps1` 逻辑，
  可自主选择启动 Python(FastAPI) / Rust(axum) / Go 后端，各自配 Vite 前端浏览器预览。
- 脚本内置端口复用保护（已运行则跳过）与后端日志隔离（backend-py / backend-rs / backend-go）。
- 修复 `.ps1` 文件 UTF-8 无 BOM 导致 Windows PowerShell 5.1 按 GBK 读取中文乱码、语法报错的问题（统一补 UTF-8 BOM）。

### 📦 其他

- 版本号 `0.3.5 → 0.3.6`（tauri.conf.json、Cargo.toml、package.json、后端 health）

## v0.3.5（2026-08-09，已打包）

> **修复「打开抖音页面失败：chrome failed to start」复发**：Edge 运行时档案迁移至系统临时目录，
> 绕开用户数据目录下 Edge headless 启动被拒（Lock file Error 5）的问题。

### 🛡️ Edge 运行时档案迁移至系统临时目录

- **背景**：0.3.3 改名 `wm-backend.exe` 后抖音恢复可用，但 0.3.4 上线后再次出现
  「打开抖音页面失败：chrome failed to start」，网页端与桌面端均不可用。
- **定位过程（对照实验逐项排除）**：
  - 同份代码 + 项目内数据目录（`.data\wmtest`）→ 稳定成功；用户数据目录（`C:\Users\21494\.watermark-tool`）→ 稳定失败
  - 失败时后端日志：`Lock file can not be created! Error code: 5`（Edge 创建 ProcessSingleton 锁被拒，Error 5 = Access Denied）
  - 独立 Go 复现程序（相同 chromedp 参数）在任何目录均成功 → 排除参数/库/进程名/数据目录本身，锁定"用户数据目录路径"相关
  - 排除项：目录 ACL 正常、无锁文件残留、无安全软件进程、Defender 受控文件夹已禁用、去掉点前缀/清空目录重建均无效
- **根因结论**：用户数据目录下 Edge headless 启动不稳定（创建 SingletonLock 被拒），
  而系统临时目录（`%TEMP%`）与项目内目录稳定可用。
- **修复**：Edge 运行时档案（`cdp_profile` 及失败重试换档目录）从数据目录迁移到
  `os.TempDir()\watermark-tool-cdp`；`dyKillStaleEdge` 清理匹配同步更新。数据/配置/
  登录态（config.json、x_login_profile、output 等）仍在原数据目录，不受影响。
- **验证**：抖音图集、抖音动图（常驻复用）、X 视频在恢复后的用户数据目录下全部解析成功。

### 📦 其他

- 版本号 `0.3.4 → 0.3.5`（tauri.conf.json、Cargo.toml、package.json、后端 health）

## v0.3.4（2026-08-09，已打包）

> **发布 0.3.3 打包后的内部迭代**：X 视频直链 tag 签名适配（修复部分视频解析后无法播放/下载的 404）、
> 解析历史增强（保存链接 / 清空确认 / 单条删除）、下载 404 友好提示。

### 🔧 X 视频直链 tag 签名适配（重要）

- **背景**：X 视频 CDN（`video.twimg.com`）的直链必须带 `tag` 播放令牌参数，且 tag 与内容分类绑定
  （实测 `amplify_video` → `tag=14`、`ext_tw_video` → `tag=29`，另有 `tag=12`），传错值返回 404。
- **现象**：部分视频能解析但无法播放/下载（404）——vxtwitter 公共 API 返回的媒体 URL 会剥离
  token（裸 URL），旧逻辑固定补 `tag=29`，遇到需要 `tag=14` 的视频即 404（选择性失败）。
- **方案**：
  1. `fetchXPublic` 改为 **fxtwitter 优先**：fxtwitter API 透传 X 原始 URL（自带正确 tag），
     从源头免去 tag 猜测；vxtwitter 仅作兜底。
  2. `xVideoURL` 直链验证回退：对任何直链先 HEAD 探测（UA + Referer `https://x.com/`）可用性，
     失败自动依次回退 `tag=29 → tag=14 → 裸 URL`，取第一个可用的。
  3. GraphQL 登录墙兜底不变（variants 中 URL 自带 tag，选最高码率档）。
- **验证**：4 条用户复现链接 + 3 条历史链接回归全部通过；6 条视频直链 HEAD 全部 HTTP 200
  （覆盖 tag=14、tag=29 及新发现的 tag=12）。

### 🆕 解析历史增强

1. **历史保存原始链接**：每条历史记录保存解析时的输入链接，支持一键复制、点击重新解析
   （直链可能随内容下架过期，重新解析可获取最新可用直链）。
2. **清空历史二次确认**：一键清空前弹出确认框（提示不可恢复），防止误触清空。
3. **单条删除**：历史列表每条记录新增删除按钮，可单独移除某一条。

### 🛡️ 下载 404 友好提示

- 下载素材遇到 HTTP 404 时给出明确提示「素材链接已失效（HTTP 404），请重新解析获取最新链接」，
  替代原先笼统的「下载失败：HTTP 404」，引导用户重新解析。

### 📦 其他

- 版本号 `0.3.3 → 0.3.4`（tauri.conf.json、Cargo.toml、package.json、后端 health）

## v0.3.3（2026-08-09）

> **修复「打开抖音页面失败：chrome failed to start」**：彻底定位为安全软件（火绒 HIPS，
> 卸载后驱动仍残留）按 `watermark-backend.exe` 文件身份拦截其创建 Edge 子进程——
> 同一份代码改名为其他文件名即可正常启动 Edge。将后端 exe 改名为 `wm-backend.exe` 规避拦截。

### 🐛 修复

1. **后端 exe 改名规避身份拦截**：实测同一代码、不同文件名，`wm-backend.exe` 可正常拉起
   Edge（完整子进程树），`watermark-backend.exe` 被安全软件拦截启动即被终止（空错误）。
   桌面端启动后端路径同步改为 `wm-backend.exe`。
2. **抖音 detail 接口签名适配**：抖音新版风控下 detail 接口强制要求 `a_bogus` 签名
   （手动页面内 XHR 无签名返回 200 空 body，页面 SDK 不再暴露签名函数），
   改为捕获详情页自身发出的带签名 detail 请求，复用其 URL 由后端立即重放获取响应，
   规避签名逆向。
3. **抖音启动自愈升级**：启动失败清理重试扩展为 3 次，固定档案连续失败自动换全新档案目录，
   绕开档案锁残留/损坏场景；启动/导航一律不加带超时的子 ctx（实测取消子 ctx 会连坐浏览器
   context 导致常驻页面失效）。

### 📦 其他

- 版本号 `0.3.2 → 0.3.3`（tauri.conf.json、Cargo.toml、package.json、后端 health）

## v0.3.2（2026-08-09）

> **修复「打开抖音页面失败：chrome failed to start」**：彻底定位为安全软件（火绒 HIPS）拦截
> Go 后端拉起的 Edge 浏览器，改用最小浏览器启动参数规避拦截。

### 🐛 修复

1. **Edge 启动参数最小化**：不再使用 chromedp 全量默认参数（其完整参数组合会触发
   安全软件 HIPS 行为拦截，导致 Edge 创建档案锁失败直接退出），改为最小参数集
   （含反检测 UA + 自动化特征隐藏 + `--no-singleton` 跳过进程锁），规避拦截。

### 🛡️ 保留的健壮性处理

- 启动前清理残留 CDP Edge 进程（进程树强杀 + 二次确认）
- 启动/重试前删除 Edge 档案锁文件（Singleton* 残留）
- 启动失败自动清理重试

### 📦 其他

- 版本号 `0.3.1 → 0.3.2`（tauri.conf.json、Cargo.toml、package.json、后端 health）

## v0.3.1（2026-08-09）

> **修复「打开抖音页面失败：chrome failed to start」**，并对升级场景做完整兜底，
> 后续升级不再需要手动关闭旧版应用。

### 🐛 修复

1. **抖音页面打开失败（chrome failed to start）**
   - 根因：残留的 CDP Edge 进程占用浏览器档案，新实例启动即失败
   - 残留清理升级：进程树强杀 + 二次确认 + 等待 800ms，确保彻底退出
   - 启动重试机制：首次启动失败 → 清理残留 → 删除档案锁文件（Singleton*）→ 重试

### 🛡️ 升级兜底（以后升级不再手动关旧版）

1. **后端启动即清理**：后端进程启动时立即清理历史残留的 CDP Edge（不依赖首次解析）
2. **升级先关旧后端**：桌面端检测到旧版后端占用端口时，先请求其优雅关闭、等待端口释放后再拉起新版，
   避免新版前端静默复用旧代码后端（旧后端无残留清理逻辑，是每次升级报错的根源）
3. **档案锁文件清理**：启动/重试前删除 Edge 的 SingletonLock / SingletonCookie / SingletonSocket 残留锁

### 📦 其他

- 版本号 `0.3.0 → 0.3.1`（tauri.conf.json、Cargo.toml、package.json、后端 health）

### ✅ 验证情况

- 沙箱实测：新版后端启动即清理残留，抖音解析正常返回；`/api/v1/shutdown` 优雅关闭（端口释放、后端退出、Edge 残留清零）

## v0.3.0（2026-08-09，已打包）

> **后端迁移 Go 重构版**：Rust 后端迁移为 Go（零第三方运行时依赖），桌面 sidecar 切换为 Go 版。
> 功能与 API 契约完全一致（前端零改动）。

### ✨ 里程碑（G1-G4 完成）

- **G1 骨架**：Go HTTP 服务（net/http 标准库）、配置读写、媒体代理（UA/Referer/Range）、SMTP 反馈、CORS
- **G2 解析核心**：XBogus 签名移植（与 Python/Rust 逐字符一致）、X 公共解析（vxtwitter/fxtwitter）、抖音短链解析
- **G3 CDP**：chromedp 浏览器管理（抖音常驻复用，二次解析 0.9s）、抖音完整解析、X 登录引导、登录墙兜底（GraphQL 监听）
- **G4 任务队列**：worker 队列 + JSON 持久化（tasks.json，避免 SQLite 体积膨胀）、link/direct 下载、Live 图双文件、进度与取消

### 📦 体积与性能

- 后端编译产物：12.5MB（零第三方运行时，chromedp 引入）
- 抖音二次解析：0.9 秒（浏览器常驻复用）
- 任务队列实测：link（解析+下载）与 direct（直链）均完成

## v0.2.6（2026-08-09，已打包）

> **设置界面新增「版本号显示 + 检查更新」**：基于 GitHub Releases 自动检查新版本。

### ✨ 新功能

1. **版本号显示**：设置抽屉「关于」区域显示当前版本（读取后端实际版本）
2. **检查更新**：点击后请求 GitHub Releases 最新版本，自动对比：
   - 有新版 → 提示版本号并提供下载页链接
   - 已是最新 → 绿色提示
   - 网络异常 → 失败提示

## v0.2.5（2026-08-09，已打包）

> **修复 X 登录误报「登录窗口已关闭」**：根因是残留的登录 Edge 实例占用档案，导致新实例启动即退出，
> 程序误判窗口关闭、反复要求登录。本次增强进程清理与启动重试。

### 🐛 修复

1. **残留登录实例清理**：改用 `taskkill /T /F` 进程树强杀 + 二次确认，确保档案不被占用；
   同时修复旧清理命令可能被安全软件拦截导致残留累积的问题
2. **启动重试机制**：Edge 启动前强制清理残留并最多重试 3 次，避免「新实例转发给旧实例后立即退出」
3. **登录轮询健壮性**：仅当「调试端口连不上 且 进程已退出」才判定窗口关闭（避免误报）；
   页面调试地址每轮刷新（登录导航/新标签不再导致轮询失效）
4. **启动失败明确提示**：浏览器 30 秒内未出现窗口时，提示可能被安全软件拦截，而非误导性的「窗口已关闭」

## v0.2.3（2026-08-09，已打包）

> **X 登录墙兜底在 Rust 后端完整落地**：M1 占位的登录引导改为真实 Edge CDP 登录流程，
> 解析端新增 GraphQL 拦截提取，登录一次后登录墙/私密推文可直接解析，无需反复登录。

### ✨ 改进（Rust 后端 M3 收尾）

1. **X 登录引导（真实 CDP 流程）**
   - 点击「打开 X 登录」→ 弹出 Edge 窗口打开 X 登录页（支持扫码/账号）
   - 后台轮询捕获 `auth_token` cookie，登录成功自动保存到 config.json 并关闭窗口
   - 固定登录档案 `x_login_profile` 持久化登录态，后续解析直接复用，无需重复登录
2. **登录墙推文解析（GraphQL 拦截）**
   - vxtwitter / fxtwitter 公共 API 失败时，自动用登录档案 + cookie 启动 headless Edge
   - 通过 CDP Network 域监听 `TweetResultByRestId` 响应，提取视频/图片媒体（兼容新旧两种响应结构）
   - cookie 注入时过滤 CDP 私有字段，避免非法参数导致整体拒绝

## v0.2.2（2026-08-08，已打包）

> 新增 X（Twitter）平台解析；修复 X 媒体 403 播放/下载、平台切换历史错乱、X 登录中途退出等问题；抖音解析与图集切换体验大幅优化。
>
> **Rust 原生重构里程碑（M1-M3 完成）**：网页端 + Windows 桌面端后端由 Python FastAPI 迁移为 Rust（axum），
> 抖音解析由 Playwright+Chromium 迁移为系统 Edge CDP，X 解析为纯 HTTP。
> 安装包从 **134MB 降至 5.1MB（-96%）**，解压体积 440MB → 18.3MB。

### 🆕 新功能

1. **X（Twitter）平台解析**
   - **平台选择器**：页面顶部新增「抖音 / X」切换，默认抖音；输入提示、解析结果、解析历史均随平台切换
   - **支持链接格式**：`x.com/<用户>/status/<ID>`、`twitter.com/...`、`x.com/i/status/<ID>`，也兼容直接粘贴 15-20 位推文 ID
   - **解析链路（三级）**：vxtwitter 公共 API 优先 → fxtwitter 兜底（均免登录、秒回）→ 配置了 X 登录 Cookie 时用 Playwright 带登录态抓取登录墙/私密推文
   - **内容类型**：视频 / 图片图集 / GIF——X 的 GIF 本质是无声 mp4，统一按视频处理并下载 mp4 文件
   - **去水印**：X 视频本身无平台水印，拿原始直链即无水印（去水印开关对 X 自动不生效）
   - **媒体播放与下载**：视频直链自动补 `tag=29` 签名参数；媒体统一经本地代理（代理按 CDN 域名带正确 Referer），保证播放/下载不被 403 拦截

2. **解析历史按平台独立**
   - 抖音与 X 的历史**分开存储**（本地存储 key：`wm_parse_history_douyin` / `wm_parse_history_x`），互不干扰
   - 切换平台时自动加载对应平台的历史；每个平台各自保留最近 50 条
   - 解析结果**始终存入发起时平台**的历史（即使解析期间切换了平台也不串）

3. **X 登录引导（登录墙内容）**
   - 设置抽屉新增「打开 X 登录」按钮，点击后自动弹出系统 Edge 窗口打开 X 登录页
   - **固定登录档案**（独立目录）：登录一次长期有效，下次直接复用，无需重输账号
   - 建议用手机 X App 扫码登录（比手输账号更稳定）
   - 登录成功后程序自动检测并保存 cookie（含 auth_token 等）到本地配置，登录墙/私密推文解析自动带上登录态
   - 未登录也能解析绝大多数公开推文；cookie 过期时解析会提示「请重新打开 X 登录」

### 🐛 修复

1. **X 视频无法播放（403）**：`video.twimg.com` 校验 Referer，浏览器直接加载直链被拒 → 媒体统一走本地代理 `/api/v1/media`，代理按 CDN 域名带正确 Referer 后播放正常
2. **X 视频下载 403**：视频直链缺少 `tag` 签名参数 → 解析时自动补 `tag=29`
3. **跨平台下载 403**：下载器原先硬编码抖音 Referer，下载 X 媒体被拒 → 改为按 CDN 域名选择合法 Referer（douyin.com / x.com）
4. **切换平台解析结果存错历史**：解析中切换平台时，结果会存到错误的平台 → 改为解析结果按发起平台保存，若已切走则提示「已存入 XX 解析历史」，解析不被打断
5. **X 登录"输到一半自动退出"**：登录窗口使用临时环境被 X 风控判定风险 → 改为固定登录档案 + 反自动化参数；登录窗口被关闭时即时反馈结果

### ⚡ 性能优化

1. **抖音解析提速（浏览器常驻复用）**
   - 原逻辑：每次解析都重新启动无头浏览器 + 打开抖音首页 + 等待安全 SDK，耗时 6-10 秒
   - 现逻辑：**首次解析**冷启动浏览器（一次性成本，约 10-20 秒）；此后浏览器**常驻复用**，后续解析直接复用页面内请求，**降到 2-3 秒**
2. **图集/视频集切换秒开（预加载）**
   - 原逻辑：切换图集项时首次加载要回源拉取，卡在原图数秒
   - 现逻辑：解析完成后**自动预加载全部图片/封面**（后台静默），切换即秒开；视频本体不预载（点击播放时才拉取，避免一次下载全部视频）
3. **浏览器生命周期管理（完整策略）**
   - **懒启动**：只有首次解析抖音时才启动无头浏览器；从不解析抖音就不启动、不占内存
   - **常驻复用**：启动后浏览器驻留后台，供后续解析复用（保证 2-3 秒解析速度）
   - **空闲自动关闭**：超过 10 分钟无抖音解析自动关闭，释放约 200-300MB 内存；下次解析自动重新启动
   - **失效自动重建**：页面失效/被平台标记时，程序自动探测并刷新重建，无需用户介入
   - **优雅退出**：应用关闭时先请求后端关闭浏览器再退出进程（超时才强杀），确保不残留 Chromium 进程
   - 运行内存：仅在「应用打开 + 10 分钟内解析过抖音」期间占用，空闲 10 分钟或关闭应用后自动释放

### 🔧 后端变更

- 新增 `parsers/x.py`（X 解析器）：vxtwitter → fxtwitter → Playwright+cookie 三级链路
- 新增端点：`/api/v1/x/login/start`（启动登录引导）、`/api/v1/x/login/status`（登录状态）、`/api/v1/shutdown`（优雅关闭）
- 新增配置项 `x_cookie`（X 登录 Cookie，敏感字段不回传前端）
- 下载与媒体代理新增 `_referer_for`：按 CDN 域名选择合法 Referer
- X 视频直链自动补 `tag=29` 签名参数
- 抖音解析器重构为浏览器常驻复用 + 空闲自动关闭（IDLE_TTL 10 分钟）
- X 解析失败按层提示：第三方服务故障 / 签名问题 / 登录态过期（引导重新登录）

### 🎨 前端变更

- HomeView：平台选择器、按平台动态 placeholder、独立历史、解析归属快照（发起平台）
- MediaViewer：媒体统一走本地代理；解析后预加载图片/封面
- App 设置抽屉：X 登录引导按钮与状态提示（未登录 / 登录中 / 已登录 / 失败）

### 📦 其他

- **Rust 原生重构（M1-M3）**：
  - 新增 `backend-rust/`：axum 后端（config/schemas/tasks/parsers/sign/diagnose/x_login），API 契约与 Python 版完全一致
  - 抖音解析：Edge CDP（`--remote-debugging-port` + `--user-agent` 固定 UA，规避 headless 验证码页）；浏览器常驻复用 + 空闲自动关闭
  - X 解析：vxtwitter/fxtwitter 纯 HTTP（tag=29、按域名 Referer）
  - XBogus（Apache 2.0）移植为纯 HTTP 备用通道签名；ABogus（GPL v3）不移植
  - 桌面壳切换到 Rust 后端：安装包 134MB → 5.1MB（-96%）；Python 打包目录退役
- web-reverse-engineering skill：扩充 `platform-parsing.md`（X 专项 + 维护与风控对抗章节）
- REFACTOR_PLAN 更新 v0.3：纳入 X 平台、明确三端关系、总约束「换语言不换逻辑」

### ✅ 验证情况

- 两个真实 X 推文链接（含敏感内容）解析成功：视频 + 封面 + 下载（mp4）
- **Rust 重构打包验证（0.2.2）**：
  - 桌面壳自动拉起 Rust 后端（health 0.2.2）；生产模式 CORS（tauri.localhost）通过
  - 生产模式实测：抖音图集 6 张解析成功、X 视频 5MB 完整下载
  - 安装包 `Watermark Tool_0.2.2_x64-setup.exe`（5.1MB，原 134MB）；解压 18.3MB（原 440MB）
  - 安装目录：`desktop/src-tauri/target/release/bundle/nsis/Watermark Tool_0.2.2_x64-setup.exe`
- X 视频经本地代理成功播放（currentTime 前进）、封面正常显示，无 403
- 平台选择器切换、独立历史（抖音空 / X 有记录）实测通过
- 登录引导：Edge 拉起、状态轮询、关闭窗口即时报错均验证通过；真实登录由用户完成并成功保存 cookie
- 抖音解析提速实测：后续解析 2.4-2.9 秒；图集切换预加载生效

---

## v0.2.1（2026-08-08）

> 修复解析后视频"无封面预览"与"无法直接播放"两个问题，核心是引入本地媒体代理绕过 CDN 防盗链。

### 🐛 修复

1. **视频 / Live 图封面预览（重要）**
   - 解析结果增加 `cover` 字段：视频取自 `video.cover`，Live 图取自 `video.cover`
   - 媒体查看器：视频使用 `<video poster>` 显示封面（与抖音页面一致的封面图），未加载时即见封面
   - 解析历史缩略图：视频 / Live 图优先显示封面，无封面时回退播放图标

2. **视频无法直接播放，只能下载（重要）**
   - 根因：抖音 CDN 防盗链要求 `User-Agent` + `Referer: https://www.douyin.com/`，而浏览器 `<video>` 无法自定义请求头，导致视频源直连被拒
   - 方案：后端新增 `/api/v1/media` 流式媒体代理，带 UA/Referer 转发素材请求
   - 代理支持 Range 分段（206 Partial Content）并透传 `content-range` / `accept-ranges`，保证视频可正常播放、拖拽、显示时长
   - 前端新增 `mediaProxyUrl()`，视频 / 图片 / 封面一律走代理地址加载

### 🔧 后端变更

- 新增 `/api/v1/media`（媒体流式代理，支持 Range）
- `/api/v1/health` 版本号对齐 0.2.1
- **Live 图 = 静态照片 + 视频 双文件**：解析结果新增 `image_url` 字段（Live 图静态照片直链），下载 Live 图时同时保存照片（.jpg/.webp）与视频（.mp4），接近抖音原生 Live 图；下载扩展名按直链 URL 推断，避免 webp 存成 jpg

### 🎨 前端变更

- **Live 图卡片**：Live 图不再直接显示视频控件，而是"静态照片 + 圆形播放按钮"，点击自动播放，播完自动回到照片，与抖音 Live 图观感一致
- 「下载当前」对 Live 图显示为"Live 图：照片+视频"，一次下载双文件
- 解析历史缩略图：Live 图优先显示静态照片

### 🐛 修复

- 修复 `manager.py` 中类内方法 `list()` 遮蔽内置 `list` 导致类型注解求值报错（引入 `from __future__ import annotations`）

### 📦 其他

- 版本号 `0.2.0 → 0.2.1`（后端 app、package.json、tauri.conf.json、Cargo.toml）

### ✅ 验证情况

- 网页版浏览器实测：解析 → 视频显示封面（poster）→ 点击播放真实播放（currentTime 推进）→ 图集切换正常 → 下载按钮可用
- 打包后端 exe 实测：health 0.2.1、解析成功（含封面字段）、打包内 headless Chromium 正常工作
- 桌面应用实测：启动自动拉起后端，生产模式跨域 API 请求（`/api/v1/config` 200）通过
- 安装包：`desktop/src-tauri/target/release/bundle/nsis/Watermark Tool_0.2.1_x64-setup.exe`

---

## v0.2.0（2026-08-08）

> 本次版本围绕"功能丰富 + 体验优化 + 关键修复"进行，主要改动在桌面版与网页版共用代码，重新打包产出 0.2.0 安装包。

### 🆕 新功能

1. **解析结果直接展示与播放**
   - 图片：解析后直接显示原图
   - 动图 / 视频 / Live 图：显示画面 + 播放按钮，点击手动播放
   - 多内容（图集 / 多图）：左右切换按钮 + 数量指示器（如 `2 / 6`）

2. **下载方式增强**
   - 「下载当前」：下载当前展示的那一个内容（新增后端 direct 直链任务类型）
   - 「全部下载」：一次下载作品全部内容（link 任务改为下载全部素材，不再只下第一个）

3. **解析历史**
   - 每次解析自动存入历史（浏览器本地存储，最多保留 50 条）
   - 历史列表显示首张缩略图 + 标题 + 类型 + 时间，一眼识别内容
   - 点击记录展开回看：可直接播放 / 切换 / 下载，支持一键清空历史

4. **单主页面整合**
   - 原「首页 + 任务中心 + 设置」三页合并为一个主页面（解析区 + 结果区 + 历史区）
   - 任务中心并入历史：下载状态随操作即时反馈，不再单独设任务页
   - 顶部新增 ⚙ 图标 → 设置抽屉：输出目录、去水印开关、问题反馈入口

5. **Bug 反馈**
   - ⚙ 抽屉 →「反馈 Bug / 建议」→ 填写内容（可选联系方式）→ SMTP 发送到开发者邮箱
   - 后端新增 `/api/v1/feedback` 接口（QQ 邮箱 SMTP）

### 🐛 修复

- **桌面版"一直加载"问题（重要）**：前端 API 地址在生产模式（Tauri 打包后）直连本地后端 `127.0.0.1:17890`，并配合后端 CORS 放行 `tauri.localhost`。网页开发模式仍走 Vite 代理，两套环境互不影响。
- 修正配置文件中带引号的输出目录值。

### 🔧 后端变更

- 任务接口支持 `direct` 类型（给定直链直接下载），`link` 类型下载全部素材
- 新增 `/api/v1/feedback`（SMTP 邮件反馈）
- `/api/v1/config` 返回时过滤 SMTP 授权码等敏感字段

### 📦 其他

- 版本号 `0.1.0 → 0.2.0`
- 删除不再使用的任务中心 / 设置页面
- 新增媒体展示组件 `MediaViewer.vue`

### ✅ 验证情况

- 网页版全流程实测通过：解析 → 媒体展示 → 切换 → 下载当前 / 全部下载 → 历史回看 → 设置抽屉 → 反馈提交
- 桌面版（0.2.0 安装包）实测：窗口启动、自动拉起后端、生产模式 API 请求与 CORS 放行验证通过
- 反馈测试邮件发送成功

---

## v0.1.x（早期版本）

- 基础解析 / 下载（抖音，视频 / 图集 / 动图 / Live 图）
- 任务队列 + 任务中心
- 一键启动脚本（浏览器 / Tauri 双模式）
- 风控诊断工具（D1-D8）
- 重构计划初稿（REFACTOR_PLAN.md）
